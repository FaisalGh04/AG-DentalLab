{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-unused-vars": [
      "warn",
      { "argsIgnorePattern": "^_", "varsIgnorePattern": "^_" }
    ]
  },
  "overrides": [
    {
      "files": [
        "src/components/admin/stage-viewer-dialog.tsx",
        "src/components/admin/case-history-dialog.tsx"
      ],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "@/hooks/use-cases",
                "message": "This dialog is READ-ONLY: it must never import a case mutation hook (useUpdateCase / useCreateCase / useDeleteCase). Note this also blocks useCase — a read — because the module exports the mutations alongside it; the PARENT owns the query and passes plain data in."
              },
              {
                "name": "@/hooks/use-confirm-action",
                "message": "This dialog is READ-ONLY: viewing history must never open the confirmation gate."
              },
              {
                "name": "@/components/admin/confirm-action-dialog",
                "message": "This dialog is READ-ONLY: viewing history must never open the confirmation gate."
              },
              {
                "name": "@/lib/prisma",
                "message": "This is a client component and must not touch the database directly."
              }
            ],
            "patterns": [
              {
                "group": ["**/hooks/use-cases", "**/hooks/use-confirm-action"],
                "message": "This dialog is READ-ONLY: relative imports of the mutation/confirmation hooks are blocked too."
              }
            ]
          }
        ]
      }
    }
  ]
}
