{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-unused-vars": [
      "warn",
      { "argsIgnorePattern": "^_", "varsIgnorePattern": "^_" }
    ]
  },
  "overrides": [
    {
      "files": ["src/components/admin/stage-viewer-dialog.tsx"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "@/hooks/use-cases",
                "message": "stage-viewer-dialog is READ-ONLY: it must never import a case mutation hook (useUpdateCase / useCreateCase / useDeleteCase)."
              },
              {
                "name": "@/hooks/use-confirm-action",
                "message": "stage-viewer-dialog is READ-ONLY: viewing a stage must never open the confirmation gate."
              },
              {
                "name": "@/components/admin/confirm-action-dialog",
                "message": "stage-viewer-dialog is READ-ONLY: viewing a stage must never open the confirmation gate."
              },
              {
                "name": "@/lib/prisma",
                "message": "stage-viewer-dialog is a client component and must not touch the database directly."
              }
            ],
            "patterns": [
              {
                "group": ["**/hooks/use-cases", "**/hooks/use-confirm-action"],
                "message": "stage-viewer-dialog is READ-ONLY: relative imports of the mutation/confirmation hooks are blocked too."
              }
            ]
          }
        ]
      }
    }
  ]
}
